#include <string.h>
#include "main.h"

/**
 *_strlen - function that return the length of a string
 *@s: parameter that we gonna check it's length
 *Return: length of a string
 */

int _strlen(char *s)
{
	size_t length = strlen(s);

	return (length);
}
